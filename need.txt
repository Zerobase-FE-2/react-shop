변경점 요약

ProductDescription.tsx 변경점

import { useDispatch } from 'react-redux';
Shopbutton onClick event 추가
<Shopbutton onClick={() => dispatch({type : "ADD", payload : {id : doc.id, count : 1}})}>장바구니에 담기</Shopbutton>

App.tsx 변경점

import { store } from './store/store'
import { Provider } from 'react-redux';
import Cart from './components/Cart';

Function App return 최상위 <Provider store={store}>로 감싸기

<Route path="/cart" element={<Cart />} />